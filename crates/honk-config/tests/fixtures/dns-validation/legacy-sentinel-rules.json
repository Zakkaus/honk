{
  "dns": {
    "upstream": [{"name": "default", "address": "192.0.2.1:53"}],
    "routing": {
      "rules": [{"domain": "example.com", "upstream": "default"}],
      "fallback": "upstream"
    }
  }
}
